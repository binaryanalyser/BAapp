import React from 'react';
import { Helmet } from 'react-helmet-async';

interface SEOHeadProps {
  title?: string;
  description?: string;
  keywords?: string;
  image?: string;
  url?: string;
  type?: string;
}

const SEOHead: React.FC<SEOHeadProps> = ({
  title = "AI Binary Analyzer - Advanced Trading Signals & Analytics for Deriv",
  description = "Professional AI-powered binary options analyzer with real-time trading signals, advanced analytics, and automated trading insights for Deriv platform. Boost your trading success with machine learning predictions.",
  keywords = "binary options, trading signals, Deriv trading, AI trading, binary analyzer, volatility index, trading analytics, automated trading, machine learning trading, binary options signals",
  image = "https://binaryanalyser.com/welcom_logo.png",
  url = "https://binaryanalyser.com",
  type = "website"
}) => {
  return (
    <Helmet>
      {/* Basic Meta Tags */}
      <title>{title}</title>
      <meta name="description" content={description} />
      <meta name="keywords" content={keywords} />
      
      {/* Open Graph */}
      <meta property="og:title" content={title} />
      <meta property="og:description" content={description} />
      <meta property="og:type" content={type} />
      <meta property="og:url" content={url} />
      <meta property="og:image" content={image} />
      
      {/* Twitter Card */}
      <meta name="twitter:card" content="summary_large_image" />
      <meta name="twitter:title" content={title} />
      <meta name="twitter:description" content={description} />
      <meta name="twitter:image" content={image} />
      
      {/* Canonical URL */}
      <link rel="canonical" href={url} />
    </Helmet>
  );
};

export default SEOHead;